import React, { PureComponent } from "react";

export default class Footer extends PureComponent {
  render() {
    return (
      <div>
        <div className="bg-secondary py-3">Footer still working</div>
      </div>
    );
  }
}
